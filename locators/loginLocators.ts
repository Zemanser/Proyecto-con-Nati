export const loginLocators = {
  loginBtnHeader: 'button:has-text("Login")', // El botón de la página principal
  userField: '#userName', // ID de usuario
  passField: '#password', // Password
  loginBtnForm: '#login', // ID del botón de login dentro del formulario
  logoutBtn: 'button:has-text("Log out")' // El botón de logout que aparece tras el login correcto
};